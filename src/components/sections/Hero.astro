---
import Button from "../ui/Button.astro";
---

<section id="inicio" class="relative bg-background-soft py-20 md:py-32 overflow-hidden min-h-screen flex items-center">
  <div class="absolute inset-0 z-0">
    <!-- Imagen para móvil -->
    <img
      src="/images/tecnico_electrofix_movil.jpg"
      alt="Técnico de Electrofix en Medellín"
      class="md:hidden w-full h-full object-cover opacity-30"
      loading="eager"
      decoding="async"
    />
    <!-- Imagen para escritorio -->
    <img
      src="/images/tecnico_electrofix.png"
      alt="Técnico de Electrofix con fondo de Medellín"
      class="hidden md:block w-full h-full object-cover opacity-30"
      loading="eager"
      decoding="async"
    />
    <div class="absolute inset-0 bg-gradient-to-r from-background-soft/95 to-transparent md:bg-gradient-to-r md:from-background-soft/95 md:via-background-soft/80 md:to-transparent"></div>
  </div>

  <div class="relative container mx-auto px-4 z-10 flex flex-col lg:flex-row items-center gap-12">
    <div class="lg:w-1/2 text-center lg:text-left">
      <h1 class="text-4xl sm:text-5xl md:text-6xl font-extrabold text-text-primary leading-tight mb-6">
        Expertos en reparación de <span class="text-primary">Electrodomésticos</span>
      </h1>
      <p class="text-xl text-text-secondary mb-8 max-w-2xl mx-auto lg:mx-0 leading-relaxed">
        Servicio técnico profesional en Medellín con <strong class="text-text-primary">Electrofix</strong>. Reparamos tu nevera, lavadora o nevecon hoy mismo. <span class="font-semibold text-primary">Garantía certificada.</span>
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center lg:justify-start">
         <Button href="#contacto" className="primary text-lg px-8 py-4 shadow-lg hover:shadow-xl transform hover:-translate-y-1">Solicitar Técnico</Button>
         <Button href="#servicios" className="bg-white text-text-primary border border-gray-200 hover:bg-gray-50">Ver Servicios</Button>
      </div>
    </div>

    <div class="lg:w-1/2 w-full max-w-md">
      <div class="bg-white/90 backdrop-blur-xl p-8 rounded-3xl shadow-2xl border border-white/50 relative overflow-hidden">
        <!-- Destello decorativo -->
        <div class="absolute top-0 right-0 w-32 h-32 bg-primary/10 rounded-full blur-2xl -mr-16 -mt-16"></div>
        
        <h2 class="text-2xl font-bold text-text-primary mb-2 text-center relative z-10">Agenda tu visita hoy</h2>
        <p class="text-text-secondary text-center mb-6 text-sm relative z-10">Revisión técnica y diagnóstico a domicilio.</p>
        
        <form class="space-y-5 relative z-10">
          <div>
            <label for="name" class="sr-only">Nombre</label>
            <input
              type="text"
              id="name"
              name="name"
              placeholder="Tu Nombre"
              class="w-full px-4 py-3 bg-white border border-gray-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent transition-all shadow-sm"
            />
          </div>
          <div>
            <label for="phone" class="sr-only">Teléfono</label>
            <input
              type="tel"
              id="phone"
              name="phone"
              placeholder="Tu Teléfono"
              class="w-full px-4 py-3 bg-white border border-gray-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-primary focus:border-transparent transition-all shadow-sm"
            />
          </div>
          <Button type="submit" className="primary w-full text-center text-lg shadow-lg">
            Solicitar técnico ahora
          </Button>
          <p class="text-xs text-center text-text-secondary mt-4">
            Respuesta rápida en menos de 15 minutos.
          </p>
        </form>
      </div>
    </div>
  </div>
</section>